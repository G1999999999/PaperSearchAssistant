# PaperSearchAssistant2 本地论文库相关论文检索方案

## 1. 文档目标

这份文档用于指导 `PaperSearchAssistant2` 增加一种明确的任务类型：

- `local_paper_search`

这类任务的目标是：

- 只从本地论文库中检索相关论文
- 列出和某个主题、关键词或方向相关的本地论文
- 返回结构化本地论文列表
- 默认不联网

本方案主要解决如下用户请求：

- 帮我列出本地库里和 3DGS 相关的论文
- 本地有哪些 Gaussian Splatting 的论文
- 看看本地库里和 NeRF 相关的论文有哪些
- 列出本地库里和 RAG evaluation 相关的论文

---

## 2. 核心原则

## 2.1 本地库相关论文检索是独立任务

这类请求不能混进以下任务中：

- 普通联网搜索
- 论文搜索（可联网）
- 某篇论文内容问答

必须单独作为：

- `local_paper_search`

## 2.2 默认只查本地，不联网

对于这类请求，默认行为是：

- 只查本地论文库
- 不自动联网

除非用户后续明确要求：

- 联网补充
- 再帮我去网上找
- 本地不够的话联网

## 2.3 返回结果应是“论文列表”，不是问答段落

这类请求的正确输出应该是：

- 本地论文列表

而不是：

- 一段知识性总结
- 一段“上下文不足”的拒答说明

---

## 3. 任务定义

## 3.1 任务名称

建议新增任务类型：

- `local_paper_search`

## 3.2 适用的用户表达

以下表达应优先识别为 `local_paper_search`：

- 帮我列出本地库里和 XX 相关的论文
- 本地有哪些 XX 的论文
- 只查本地，看看有哪些 XX 论文
- 看看我本地库里和 XX 相关的论文
- 列出本地库里所有 XX 方向的论文

## 3.3 与其他任务的区别

### 与 `paper_search` 的区别

`paper_search`：

- 默认本地优先
- 本地不足时可联网补充

`local_paper_search`：

- 只查本地
- 默认不联网

### 与 `paper_qa` 的区别

`paper_qa`：

- 问某篇论文内容
- 要进入论文内部检索

`local_paper_search`：

- 只是列出相关本地论文
- 不进入论文内容问答

---

## 4. 正确的产品行为

对于：

- `帮我列出本地库里和 3DGS 相关的论文`

正确行为应为：

1. 路由识别为 `local_paper_search`
2. 只查询本地论文库
3. 在本地库中按标题、摘要、关键词等匹配
4. 返回本地论文列表
5. 不联网

如果本地没有结果，则返回：

- 本地库中暂无相关论文

并可选提示：

- 若需要，我可以再帮你联网搜索

---

## 5. 本地论文检索范围

这类任务应只使用本地数据源：

- 本地论文主表
- 本地论文摘要
- 本地标题索引
- 本地 PDF 记录
- 本地已建索引论文

不能默认使用：

- arXiv
- 网页搜索
- 外部论文搜索引擎

---

## 6. 本地论文检索算法

为了让“本地相关论文列表”更准确，建议本地搜索至少包含以下几层。

## 6.1 标题匹配

支持：

- 精确标题匹配
- 规范化标题匹配
- 模糊标题匹配

## 6.2 摘要匹配

在本地论文摘要中匹配主题关键词。

这对以下问题很有用：

- 某主题论文
- 某任务论文
- 某技术路线论文

## 6.3 关键词 / BM25 / FTS 匹配

本地论文搜索建议启用：

- PostgreSQL FTS
- BM25 风格匹配

适合：

- 术语
- 缩写
- 方法名
- 任务名

例如：

- 3DGS
- Gaussian Splatting
- NeRF
- RAG

## 6.4 向量召回（可选）

如果用户查询比较概念化，且本地论文量已经较大，可以增加：

- 本地论文摘要向量召回

但返回层面仍然是“论文列表”，不是 chunk 级内容问答。

推荐用途：

- 补充召回
- 改善语义相关性

## 6.5 混合排序

推荐排序信号包括：

- 标题匹配得分
- 摘要匹配得分
- FTS / BM25 得分
- 向量相似度得分
- 论文新鲜度

---

## 7. 推荐返回字段

对于每条本地论文结果，建议返回：

- 标题
- 作者
- 摘要
- 发布时间
- arXiv ID
- 本地 PDF 路径
- 是否已建全文索引
- 来源类型 `local`

推荐结构：

```json
{
  "title": "...",
  "authors": ["A", "B"],
  "summary": "...",
  "published": "...",
  "arxiv_id": "...",
  "pdf_path": "...",
  "indexed": true,
  "source_type": "local"
}
```

---

## 8. 空结果时的行为

如果本地库没有匹配结果，不能回答：

- 当前上下文不足

正确行为应为：

- 明确告知“本地论文库中未找到相关论文”

可选附加一句：

- 如果需要，我可以继续联网帮你搜索

也就是说：

- 默认仍然只查本地
- 但允许后续用户触发联网扩展

---

## 9. 用户后续动作支持

本地论文列表返回后，系统应支持用户继续说：

- 读第 1 篇
- 说一说第 2 篇
- 打开这篇
- 比较前两篇论文

因此，`local_paper_search` 的结果需要能够自然衔接到：

- `paper_read`
- `paper_qa`

推荐做法：

- 列表中的每条结果带唯一 `paper_id`
- 支持 session 内候选编号引用

---

## 10. 不应出现的错误行为

## 10.1 错误行为一：把本地论文检索当成普通 RAG 问答

不能因为当前上下文没有论文片段，就回答：

- 当前上下文不足

## 10.2 错误行为二：本地查询时自动联网

对于“列出本地库里...”这类请求，不能默认联网。

## 10.3 错误行为三：不返回结构化论文列表

不能只返回一段概括性文字，而不列出论文条目。

## 10.4 错误行为四：把本地论文检索和本地论文问答混在一起

用户只是想列出相关论文时，不要直接进入某篇论文内容分析。

---

## 11. 推荐路由输出

对于：

- `帮我列出本地库里和 3DGS 相关的论文`

推荐输出：

```json
{
  "intent": "local_paper_search",
  "topic_query": "3DGS 3D Gaussian Splatting",
  "local_only": true,
  "allow_web_fallback": false,
  "confidence": 0.98
}
```

关键点：

- `intent = local_paper_search`
- `local_only = true`
- `allow_web_fallback = false`

---

## 12. 推荐新增模块

建议新增：

- `tools/retrieval/local_library_search_service.py`
- `tools/retrieval/local_library_ranker.py`

职责如下：

## 12.1 `local_library_search_service.py`

- 只面向本地论文库检索
- 输出结构化本地论文列表

## 12.2 `local_library_ranker.py`

- 对本地候选结果做排序
- 综合标题、摘要、FTS、向量等得分

---

## 13. 推荐修改的现有模块

## 13.1 `router.py`

增加：

- `local_paper_search`

并确保以下表达能稳定命中：

- 本地有哪些...
- 列出本地库里...
- 只查本地...

## 13.2 `agent.py`

增加：

- `local_paper_search` 的独立处理分支

返回：

- 本地论文列表

而不是普通问答回答。

## 13.3 `paper_search_service.py`

建议拆分：

- 可联网的 `paper_search`
- 仅本地的 `local_paper_search`

---

## 14. 推荐给 Cursor 的实施顺序

建议 Cursor 按以下顺序实施：

1. 新增 `local_paper_search` 意图
2. 增加本地库专用搜索服务
3. 接入标题 / 摘要 / FTS / 可选向量混合检索
4. 返回结构化本地论文列表
5. 支持从本地列表继续进入 `paper_read` 和 `paper_qa`

---

## 15. 最重要的结论

对于：

- `帮我列出本地库里和某某相关的论文`

系统必须做到：

1. 识别为 `local_paper_search`
2. 只查询本地论文库
3. 返回结构化本地论文列表
4. 不默认联网

一句话总结：

- **“列出本地相关论文”必须成为一个独立任务，而不是论文搜索或论文问答的附属行为。**

