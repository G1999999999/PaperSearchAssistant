# PaperSearchAssistant2 面试问答（参考答案）

> 用法建议：每题先讲“结论一句话”，再补“我怎么做的”，最后加“效果/反思”。

## 1) 项目定位与架构全景

### Q1. 你先用 2 分钟介绍这个项目解决了什么问题，核心用户是谁？
**A：**  
`PaperSearchAssistant2` 是一个面向“论文检索 + 本地论文问答”的后端系统。它解决的是：用户不仅要找论文，还要把论文下载、入库、向量化，然后基于证据做问答。核心用户是研究生、算法工程师和需要快速阅读论文的开发者。  
我把能力做成一条闭环：`arXiv 检索 -> PDF 下载 -> 本地索引 -> 语义检索 -> RAG 回答`，并且支持会话记忆和工具调用，保证从“找资料”到“可复用知识库”的连续体验。

### Q2. 为什么选择 FastAPI 单体而不是拆成多服务？
**A：**  
这个阶段我优先考虑迭代效率和可演示性，所以用 FastAPI 单体。检索、向量、论文库、Agent 编排都在同一进程，调试链路短、上下文一致，适合快速试验检索策略。  
当规模增大时，我会先拆高负载模块，比如“批量嵌入任务”和“在线问答服务”，再做异步队列化。

### Q3. 从用户提问到最终答案的关键步骤是什么？
**A：**  
主流程是：  
1. 规范化问题并做 query 扩展。  
2. 按策略检索（向量/混合/重排）。  
3. 组装上下文和引用。  
4. 调用 LLM 生成答案。  
5. 返回 `answer + citations`，并可写入会话记忆。  
如果问题里带 arXiv id，会走短路流程自动完成论文下载与问答，减少用户操作成本。

### Q4. 你这个项目和普通 RAG Demo 的本质差异是什么？
**A：**  
不是“只做问答”，而是“论文工作流系统”。差异有三点：  
- 有本地论文库（SQLite）和论文生命周期管理。  
- 检索策略可切换并可解释（hybrid、rerank、多路融合）。  
- 有工程化能力：审批流、审计日志、中间件、会话与长期记忆。

### Q5. 你会怎么划分模块边界？
**A：**  
- API 层：参数校验、协议转换、流式输出。  
- Agent/RAG 层：路由、检索策略、提示词与工具编排。  
- 检索层：向量库、BM25、RRF、重排。  
- 存储层：SQLite 元数据、Chroma 向量、文件系统 PDF。  
- 治理层：中间件、审批、审计、统计。

---

## 2) 检索与 RAG 设计

### Q6. 为什么选 Chroma + Qwen Embeddings？
**A：**  
Chroma 本地部署轻、开发门槛低，适合单机迭代；Qwen Embeddings 和主模型生态一致，调用链路统一。  
我当时目标是“稳定跑通 + 快速试策略”，所以优先本地可控性和接入效率，而不是先上分布式向量数据库。

### Q7. `default / hybrid / hybrid_rerank / rerank` 各适用什么场景？
**A：**  
- `default`：低延迟，适合普通问答。  
- `hybrid`：语义 + 关键词，适合术语精确匹配场景。  
- `rerank`：重视精准 Top-K，适合高价值问题。  
- `hybrid_rerank`：质量优先默认策略，召回和精排都兼顾。  
我的线上默认是 `hybrid_rerank`，并开放参数给调用方按成本/时延切换。

### Q8. query expansion 的收益和风险？
**A：**  
收益是召回更稳，尤其同义表达和中英文混合时。风险是 query drift（扩展偏题）。  
控制手段：限制扩展数量、保留原始 query 权重、最终靠重排模型和阈值过滤兜底。

### Q9. `score_threshold` 怎么定？
**A：**  
先用经验值启动，再根据离线样例做网格调参。  
我会看两个指标：命中率（至少有一条强相关证据）和幻觉率（无效证据却强答），选择平衡点。不同 namespace 未来会做自适应阈值。

### Q10. 你如何验证检索改进确实有效？
**A：**  
做固定评测集对比不同策略，比较 Hit@K、MRR 和人工打分；并记录回答是否引用到正确段落。  
改进必须同时满足“质量提升”和“延迟可接受”，否则不会切默认策略。

---

## 3) 文档处理与索引构建

### Q11. chunk 参数怎么选？
**A：**  
`chunk_size` 太小会丢上下文，太大会稀释语义；`overlap` 用来保留跨段信息。  
我采用中等粒度作为默认值，并在论文问答场景做过对比，最终选择“质量和成本都可接受”的折中配置。

### Q12. PDF 解析最难的坑是什么？
**A：**  
公式、图表、双栏排版和断行会影响语义完整性。  
我用了专门的 PDF 分块逻辑，并补充图像/图注嵌入能力，让检索证据不只来自正文文本。

### Q13. 为什么要做增量嵌入和 manifest？
**A：**  
避免每次全量重建，节省大量 embedding 成本和时间。  
通过文件哈希、chunk 参数、namespace 共同判定是否需要重建，实现幂等且可追踪的增量更新。

### Q14. 并发嵌入怎么保证一致性？
**A：**  
我把“PDF 分块”并行化，把“向量写入”串行化并加锁，避免 collection 并发写冲突。  
这样既利用了 CPU 并行能力，也保证了索引状态一致。

### Q15. 从 100 篇扩到 10 万篇，哪里先扛不住？
**A：**  
先扛不住的是本地单机 I/O 和向量检索吞吐，其次是重排成本。  
演进路径：向量库外置化、嵌入任务队列化、检索缓存、分层召回、重排按需触发。

---

## 4) 多源能力与 Agent

### Q16. 工具调用是怎么触发的？
**A：**  
`use_tools=true` 时，模型可基于意图选择工具（论文检索、知识库检索、天气等）。  
我做了工具白名单和结果回填，避免工具输出失控，并在必要时走人工审批。

### Q17. 什么时候走 `answer`，什么时候走 `answer_with_tools`？
**A：**  
纯知识库问答优先 `answer`，延迟低、可控性高；需要外部信息或复杂任务分解时用 `answer_with_tools`。  
如果用户上传图片，我会优先主 RAG 流，先做相关性判断再决定是否注入多模态上下文。

### Q18. 怎么防止工具循环调用？
**A：**  
设置最大调用步数与终止条件，且每轮都检查是否仍有有效 tool call。  
同时使用中间件做调用计数限制，超过阈值就中断并返回可解释结果。

### Q19. 为什么要审批流？
**A：**  
高风险工具（如删除文件）不能直接由模型执行。  
审批流把“模型建议”变成“人机共决策”，可以 approve/edit/reject，并写审计日志，满足安全和可追溯要求。

### Q20. LangGraph 开关的价值是什么？
**A：**  
它是编排能力的可切换实验位。  
我可以在不破坏现有链路的前提下，对比“轻量流程 vs 图编排流程”的稳定性和可维护性，降低架构演进风险。

---

## 5) 数据与存储层

### Q21. 为什么要 SQLite + Chroma 双存储？
**A：**  
两者职责不同：SQLite 适合结构化过滤（作者、年份、关键词），Chroma 适合语义召回。  
双存储把“精确筛选”和“语义理解”组合起来，比单一检索更稳。

### Q22. 会话历史和长期记忆怎么区分？
**A：**  
会话历史用于当前对话连贯性（短期上下文），长期记忆用于跨会话经验复用（向量化存档）。  
这样能控制 prompt 长度，同时保留长期可检索知识。

### Q23. namespace 怎么设计防止污染？
**A：**  
按业务域/论文/会话分层命名，例如 `paper:<id>:full`。  
检索时严格在目标 namespace 内执行，避免跨域误召回。

### Q24. 如果支持多租户会怎么改？
**A：**  
在 namespace 和元数据中加入 tenant_id 作为强隔离主键；  
API 层做鉴权与租户上下文注入；  
存储层引入租户级配额和审计策略。

---

## 6) 可靠性、安全性、工程化

### Q25. 你做了哪些安全防护？
**A：**  
包括空输入校验、超长限制、上传大小限制、文件名安全化、防路径穿越、异常统一处理。  
这能覆盖最常见的输入攻击面和资源滥用风险。

### Q26. 为什么要 PII 脱敏？
**A：**  
避免敏感信息直接进入模型上下文或日志，降低合规风险。  
我把它放在中间件层，这样不依赖具体业务路径，所有入口都能复用同一策略。

### Q27. 全局异常处理会不会隐藏问题？
**A：**  
对外统一错误是为了安全和体验；对内通过日志与审计保留细节用于排障。  
生产环境会进一步区分业务异常和系统异常，避免“全部 500”影响可观测性。

### Q28. 删除文件审批如果系统挂了怎么办？
**A：**  
策略是“失败即拒绝执行”，宁可不删也不误删。  
审批不可用时返回明确错误，让调用方重试或人工介入，保证安全优先。

### Q29. 你有哪些可观测性手段？
**A：**  
请求统计、调用计数、审批记录、审计日志、失败信息落盘。  
出了问题可以追到“哪次请求、哪次工具调用、哪次决策”。

---

## 7) API 与前端协同

### Q30. 为什么同时有普通问答和流式问答接口？
**A：**  
普通接口适合后端服务调用，流式接口适合前端实时交互。  
两者复用同一核心执行逻辑，避免分叉实现导致行为不一致。

### Q31. 为什么做 multipart 接口？
**A：**  
图片上传场景用 multipart 更直接，能减少前端 base64 编码开销。  
同时保留 JSON 接口，兼容纯文本调用方，保证接入灵活性。

### Q32. 前端如何保证 answer 和 citations 一致？
**A：**  
后端同一次生成返回结构化 `answer + citations`，前端只做渲染不做二次推断。  
流式场景先发 meta 再发 delta，保证引用信息先就位。

---

## 8) 性能与成本

### Q33. 最大性能瓶颈在哪里？
**A：**  
通常是 LLM 生成和 rerank，其次是大 PDF 嵌入。  
所以我把“离线重任务”和“在线问答”解耦，并提供策略开关按需降本。

### Q34. 你做了哪些降本提速手段？
**A：**  
增量嵌入、候选集裁剪、按需重排、阈值过滤、策略可配。  
核心思想是：高成本能力默认不开满，只在必要时触发。

### Q35. 如果要把 P95 降 30%，你先做什么？
**A：**  
先做链路分段 profiling，确认是检索慢还是生成慢。  
通常第一步会降低无效候选和重排频率；第二步做缓存和并行优化；第三步再考虑模型级优化。

---

## 9) 评估与效果证明

### Q36. 你怎么定义“变好”？
**A：**  
不是只看主观回答流畅，而是看“检索命中 + 回答正确 + 引用可靠”。  
我会同时看 Hit@K、MRR、人工相关性打分和幻觉率。

### Q37. 你如何构建评测集？
**A：**  
从真实问题中抽样，覆盖术语问法、跨段推理、时间过滤、多论文对比等难样本。  
评测集固定后做回归测试，避免策略升级带来隐性退化。

### Q38. 如何证明 `hybrid_rerank` 更好？
**A：**  
同一数据集上做策略对照，比较前 K 命中和最终答案质量。  
如果质量提升显著且时延在可接受范围，我才会把它设为默认。

---

## 10) 个人贡献与反思

### Q39. 你最核心的 3 个技术决策？
**A：**  
1. 用“双存储 + 多策略检索”保证效果上限。  
2. 用“审批 + 审计 + 中间件”保证可控和可运维。  
3. 用“单体先跑通 + 开关化演进”保证迭代速度和后续可扩展性。

### Q40. 最大的坑和你的解决方式？
**A：**  
最大坑是“检索看起来有结果，但答案仍不稳”。  
我通过 query 扩展、混合召回、重排和阈值控制组合治理，最终把“有证据却答偏”的情况明显降低。

### Q41. 如果重做一次你会改什么？
**A：**  
我会更早建立标准评测集和自动化回归，把“策略改动 -> 效果变化”量化得更快。  
第二是更早拆分离线任务系统，避免后期批处理和在线服务耦合。

### Q42. 下一步演进方向？
**A：**  
优先做三件事：  
- 多租户与权限隔离。  
- 更系统的评测与监控仪表盘。  
- 多模态检索优化（图表/公式证据利用更充分）。

---

## 面试时可直接背的 30 秒总结

`PaperSearchAssistant2` 不是单点 RAG Demo，而是一个论文工作流系统：支持 arXiv 检索下载、本地论文库管理、向量检索、混合召回与重排、再到可追溯的问答输出。我在设计上重点平衡了三个目标：效果（多策略检索）、效率（增量嵌入与可配策略）和安全可控（审批流、中间件、审计日志）。如果业务规模增长，我的演进路径也很明确：离线任务队列化、存储外置化、多租户隔离和自动化评测。
